/**
 * StateProvider.js — Central source of truth
 */
import { sharedState } from '../stateExports.js';

// For now, we wrap the existing sharedState to maintain compatibility with main.js
export { sharedState };
